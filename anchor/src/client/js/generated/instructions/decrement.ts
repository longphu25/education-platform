/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from 'gill';
import { EDUAPP_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DECREMENT_DISCRIMINATOR = new Uint8Array([
  106, 227, 168, 59, 248, 27, 150, 101,
]);

export function getDecrementDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DECREMENT_DISCRIMINATOR);
}

export type DecrementInstruction<
  TProgram extends string = typeof EDUAPP_PROGRAM_ADDRESS,
  TAccountEduapp extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEduapp extends string
        ? WritableAccount<TAccountEduapp>
        : TAccountEduapp,
      ...TRemainingAccounts,
    ]
  >;

export type DecrementInstructionData = { discriminator: ReadonlyUint8Array };

export type DecrementInstructionDataArgs = {};

export function getDecrementInstructionDataEncoder(): FixedSizeEncoder<DecrementInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: DECREMENT_DISCRIMINATOR })
  );
}

export function getDecrementInstructionDataDecoder(): FixedSizeDecoder<DecrementInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDecrementInstructionDataCodec(): FixedSizeCodec<
  DecrementInstructionDataArgs,
  DecrementInstructionData
> {
  return combineCodec(
    getDecrementInstructionDataEncoder(),
    getDecrementInstructionDataDecoder()
  );
}

export type DecrementInput<TAccountEduapp extends string = string> = {
  eduapp: Address<TAccountEduapp>;
};

export function getDecrementInstruction<
  TAccountEduapp extends string,
  TProgramAddress extends Address = typeof EDUAPP_PROGRAM_ADDRESS,
>(
  input: DecrementInput<TAccountEduapp>,
  config?: { programAddress?: TProgramAddress }
): DecrementInstruction<TProgramAddress, TAccountEduapp> {
  // Program address.
  const programAddress = config?.programAddress ?? EDUAPP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    eduapp: { value: input.eduapp ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.eduapp)],
    data: getDecrementInstructionDataEncoder().encode({}),
    programAddress,
  } as DecrementInstruction<TProgramAddress, TAccountEduapp>);
}

export type ParsedDecrementInstruction<
  TProgram extends string = typeof EDUAPP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    eduapp: TAccountMetas[0];
  };
  data: DecrementInstructionData;
};

export function parseDecrementInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDecrementInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { eduapp: getNextAccount() },
    data: getDecrementInstructionDataDecoder().decode(instruction.data),
  };
}
